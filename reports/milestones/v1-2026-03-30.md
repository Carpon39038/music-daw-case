# Milestone v1 (2026-03-30)

## Definition

目标：达到并稳定维持以下条件：

1. 可编辑 clip 位置（拖拽 + 拍点吸附 + 边界限制）
2. 播放稳定（transport、调度与清理行为通过回归）
3. 全绿验证（verify:full PASS）
4. 一键回归（verify:ci + harness loop 产物齐全）

## Evidence

- Drag/snap/bounds: `src/App.tsx` + e2e `clip drag should snap to beat and clamp within both timeline bounds`
- Playback schedule consistency: e2e `clip drag position should be reflected in playback schedule state`
- Drag cancel consistency: e2e `clip drag should revert on Escape (cancel consistency)`
- Runtime cleanup: e2e `audio runtime should clear scheduled nodes on pause and stop`
- Latest verify task: `verify-2026-03-30` (PASS)

## Decision

Milestone v1 criteria met. Ready to tag `v1.0.0`.

## Post-v1 Backlog (suggested)

- clip overlap strategy (allow/forbid/smart-resolve)
- persistent project storage
- richer undo/redo model
- branch protection rollout confirmation in repo settings
